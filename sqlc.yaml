version: "2"

sql:
  - engine: "postgresql"
    schema: "db/schema"
    queries: "db/sqlc"

    gen:
      go:
        package: "queries"
        out: "db/queries"
        sql_package: "database/sql"
